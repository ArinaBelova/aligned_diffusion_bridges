name: sbalign
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - pytorch==2.4.0 # for now want the newest pytorch to see which opencv will be installed, it'll be 2.5.1; i have this version for the aligned experiments: ==2.4.0
  - pip
  - pip:
    - torch-ema
    - easydict
    - matplotlib
    - seaborn
    - wandb
    - jupyter
    - termcolor
    - prefetch_generator
    - biopython
    - biopandas
    - openpyxl
    - omegaconf
    - einops
    - scikit-learn
    - e3nn
    - opencv-python-headless==4.10.0.84  #==4.6.0.66
    - lmdb
    - lightning
    # - mkl==2022.1.0
    # - libglib2.0-0 
    # - libgl1